import {Wrapper} from "./App.styles";
import {_Nav} from "./components/Nav/_Nav";

export const App = (props) => {
  return (
      <Wrapper>
      <_Nav/>
          </Wrapper>
  );
}

export default App;
